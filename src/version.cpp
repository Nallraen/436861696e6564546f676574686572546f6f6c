#include "version.hpp"

namespace big
{
	const char* version::GIT_SHA1           = "";
	const char* version::GIT_BRANCH         = "";
	const char* version::GIT_DATE           = "";
	const char* version::GIT_COMMIT_SUBJECT = "";
};
